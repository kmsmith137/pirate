# scratch

For debugging: run whatever code is currently in `src_lib`/scratch.cu

```{code-block} text
:class: cli-help

usage: pirate_frb dev scratch [-h]

For debugging: run whatever code is currently in src_lib/scratch.cu

options:
  -h, --help  show this help message and exit
```
